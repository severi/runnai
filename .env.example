# Optional if using `claude login` (Pro/Max subscription).
# Set this for API billing instead.
ANTHROPIC_API_KEY=
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
# Optional: fallback if OAuth tokens file doesn't exist
STRAVA_REFRESH_TOKEN=
