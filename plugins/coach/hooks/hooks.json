{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PLUGIN_ROOT}/../..\" && if [ -d data/.git ]; then git -C data add -A && git -C data commit -m 'Auto-save: data update' --allow-empty 2>/dev/null || true; fi"
          }
        ]
      }
    ]
  }
}
